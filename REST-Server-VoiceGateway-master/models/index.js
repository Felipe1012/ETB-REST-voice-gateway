const User = require('./user');
/*var admin = new User({ "email": "admin", "role": "admin", "password": "1234567" })
admin.save(function (err) {
  if (err)  throw err;
  console.log('meow');
});*/
module.exports = {
    User
}